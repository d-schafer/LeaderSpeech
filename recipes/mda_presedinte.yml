# Moldova — Presidency of the Republic (presedinte.md), President Maia Sandu (archive also holds
# Dodon 2016-2020 and Timofti 2012-2016). Plain static site, but the TLS chain does not validate
# from outside .md (CERTIFICATE_VERIFY_FAILED) -> verify_ssl: false.
#
# STRUCTURE: /discursuri is a year archive. Each year is a CakePHP-paginated listing at
# /rom/discursuri/index/YYYY/page:N (~7 speeches/page; page:1 == the bare /discursuri/YYYY page;
# past the last page returns 0 links so the engine auto-stops). Individual speeches live at
# /rom/discursuri/<slug>. We enumerate the per-year index bases as start_urls and let path
# pagination walk page:1..N of each year. Detail pages carry title (h1), body (<article>), and a
# machine-readable date (<time datetime>).
source_id: mda_presedinte
country: Moldova
source_language: Romanian
verify_ssl: false
renderer: static
start_urls:
  - https://presedinte.md/rom/discursuri/index/2013
  - https://presedinte.md/rom/discursuri/index/2014
  - https://presedinte.md/rom/discursuri/index/2015
  - https://presedinte.md/rom/discursuri/index/2016
  - https://presedinte.md/rom/discursuri/index/2017
  - https://presedinte.md/rom/discursuri/index/2018
  - https://presedinte.md/rom/discursuri/index/2019
  - https://presedinte.md/rom/discursuri/index/2020
  - https://presedinte.md/rom/discursuri/index/2021
  - https://presedinte.md/rom/discursuri/index/2022
  - https://presedinte.md/rom/discursuri/index/2023
  - https://presedinte.md/rom/discursuri/index/2024
  - https://presedinte.md/rom/discursuri/index/2025
  - https://presedinte.md/rom/discursuri/index/2026
  # Press releases — the second article stream, same pager, added 2026-08-13 (A6). The year index
  # on /rom/comunicate-de-presa offers 2012 onward, two years deeper than the speeches section.
  - https://presedinte.md/rom/comunicate-de-presa/index/2012
  - https://presedinte.md/rom/comunicate-de-presa/index/2013
  - https://presedinte.md/rom/comunicate-de-presa/index/2014
  - https://presedinte.md/rom/comunicate-de-presa/index/2015
  - https://presedinte.md/rom/comunicate-de-presa/index/2016
  - https://presedinte.md/rom/comunicate-de-presa/index/2017
  - https://presedinte.md/rom/comunicate-de-presa/index/2018
  - https://presedinte.md/rom/comunicate-de-presa/index/2019
  - https://presedinte.md/rom/comunicate-de-presa/index/2020
  - https://presedinte.md/rom/comunicate-de-presa/index/2021
  - https://presedinte.md/rom/comunicate-de-presa/index/2022
  - https://presedinte.md/rom/comunicate-de-presa/index/2023
  - https://presedinte.md/rom/comunicate-de-presa/index/2024
  - https://presedinte.md/rom/comunicate-de-presa/index/2025
  - https://presedinte.md/rom/comunicate-de-presa/index/2026

politeness:
  # presedinte.md rate-limits by IP and enforces it by DROPPING packets, so a blocked request
  # shows up as a connect timeout (WinError 10060) rather than an HTTP status. RAISED 2026-08-08
  # from [0.5, 1.2] / 25 / 5s, which was not enough: on 2026-08-07 the ~216-request HARVEST alone
  # tripped the limit at ~1.6s spacing, and every detail fetch that followed timed out until the
  # 25-consecutive-failure breaker aborted the run with 2 speeches scraped. The block is
  # temporary, not a permanent ban — the same URLs served 200 the next day, and 240 consecutive
  # fetches at 3s spacing completed with zero failures.
  #
  # CORRECTED 2026-08-08 (same day, after the run proved the first fix wrong). The quota is
  # CUMULATIVE, not a rate — pacing postpones it, it does not avoid it. The 2.0-3.5s run scraped
  # 225 items consecutively with ZERO failures and then blocked anyway, at ~250 total requests;
  # the 240-request probe that "passed clean" had simply stopped just under the same cap.
  #
  # `retries: 5, backoff: 30` was actively HARMFUL here. Once blocked, every URL burned 5
  # attempts across 9m18s (measured: consecutive errors exactly 9m18s apart) while still hitting
  # a host that was dropping packets, so the run took 8.3 hours to land 247 speeches and 2.3
  # hours just to accumulate the 25 consecutive failures that abort it. Fail fast instead:
  # 3 attempts / 20s base = 140s, and let --retry-failed pick them up on a later run.
  # (Contrast rou_guvern, where the ban is a ~6-minute TLS reject and riding it out DOES work —
  # same-looking symptom, opposite remedy.)
  #
  # THIS RECIPE IS NOW A TOP-UP, NOT THE MAIN HARVEST. mda_presedinte_wayback carries the
  # history (1,327 of the live site's 1,378 slugs, no quota). Run the wayback recipe FIRST; the
  # shared state then leaves this one only the ~89 recent live-only items. Cap the listing crawl
  # so the harvest itself does not eat the quota — new items sit on the first pages of each year:
  #     ... text_scraper.run --recipe recipes/mda_presedinte.yml --max-pages 3
  # 14 start_urls x 3 pages = 42 listing requests + ~89 details, comfortably under the cap.
  delay_range: [2.0, 3.5]
  pause_every: 50
  pause_seconds: 30
  retries: 3
  backoff: 20

pagination:
  # CakePHP page:N pager appended to each year-index base; each year self-terminates when a page
  # yields no new speech links.
  type: path
  path_format: "page:{n}"
  start: 1
  step: 1
  # ⚠ 30 → 3, 2026-08-13. THIS IS A QUOTA BUDGET, NOT A PAGER GUESS, and it is the one knob that
  # keeps this recipe runnable at all. presedinte.md enforces a CUMULATIVE per-IP quota of roughly
  # 225-250 requests and then blackholes the client, whatever the pacing (measured 2026-08-08/10;
  # see claude_log.md "the Moldova diagnosis was wrong"). max_pages is PER START_URL, so with 29
  # year-listings a cap of 30 authorises up to 870 listing requests — 3.5x the quota — before a
  # single article is fetched. At 3 it is ~87, which leaves room for the detail fetches.
  # The 2026-08-10 fix made THE WAYBACK RECIPE THE PRIMARY ROUTE and this one a TOP-UP for the
  # ~89 live-only recent items; that decision was recorded only as a `--max-pages 3` command-line
  # flag in the log, so it was invisible here and the A6 widening walked straight into it. It now
  # lives in the recipe. Run mda_presedinte_wayback FIRST, then this.
  max_pages: 3

listing:
  # Individual speeches only: /rom/discursuri/<slug>. Exclude the /discursuri/<year> index links
  # and the /discursuri/index/<year>/page:N pager links.
  # ⚠ THE WIDENING IS NOT --spread PROBE-VALIDATED, and here is exactly why (2026-08-13).
  # presedinte.md rate-limits by IP with a CUMULATIVE quota — roughly 225-250 requests, then it
  # blackholes the client regardless of pacing (see mda_presedinte_wayback.yml's header, which
  # measured this in detail on 2026-08-08). A --spread probe over 30 year-listings exceeds that
  # by construction: it ran 38 minutes and returned nothing before being stopped.
  # WHAT *IS* VERIFIED, without a full probe:
  #   * /rom/comunicate-de-presa/index/2024 returns a real listing of real items under the honest
  #     bot UA, item links shaped /rom/comunicate-de-presa/<slug> — identical to discursuri.
  #   * the year index on /rom/comunicate-de-presa offers 2012 onward.
  #   * the WAYBACK sibling, which uses this same widened section list and the same selectors,
  #     re-probed at 6,564 snapshots with title 12/12, text 12/12, date 12/12.
  # ⇢ AND NOTE `--spread` IS THE WRONG PROBE FOR THIS SOURCE. It walks every listing page of every
  #   start_url, which is precisely the thing the quota forbids. Use a bounded probe instead:
  #     python -m leaderspeech.text_scraper.probe --recipe recipes/mda_presedinte.yml --n 8
  #   (no --spread), which respects max_pages: 3 and stays inside the budget.
  #
  # ⚠ WIDENED 2026-08-13 (sweep chunk A6): the recipe took the section literally called
  # "Discursuri" — the RULE 1 shape. presedinte.md's nav has a second, larger article stream,
  # `comunicate-de-presa` (press releases / "Comunicate de presă"), with the identical
  # /rom/<section>/index/<YEAR> pager and the identical /rom/<section>/<slug> item shape,
  # covered by no recipe. Verified live: /rom/comunicate-de-presa/index/2024 lists real items
  # ("Președinta Maia Sandu a discutat despre situația energetică…").
  link_pattern: '/(?:discursuri|comunicate-de-presa)/(?!index|\d{4})[a-z]'

title:
  selectors: ["h1"]
text:
  selectors: ["article", ".content", "main"]
date:
  selectors: ["time", ".date"]
  attr: datetime

position: president
date_languages: ["ro"]
notes: >
  DRAFT — probe-validated (no full run). Moldova Presidency (Maia Sandu; archive also holds Dodon
  and Timofti) via presedinte.md. NEW master row needed (Moldova had no row). Static, broken TLS ->
  verify_ssl:false. Per-year CakePHP listings /rom/discursuri/index/YYYY/page:N walked by path
  pagination (~7/page, auto-stops at the first empty page); individual speeches /rom/discursuri/<slug>.
  Title h1, body <article>, date from <time datetime> (ISO). Original language Romanian (an /eng
  mirror exists but is not used). Speaker left to the cleaner's tenure crosscheck (multiple presidents
  in the archive). FULL RUN pending.
