# Recipes and the generated source list are text; pin them to LF so a Windows checkout
# doesn't produce CRLF-only diffs.
# (The old data/sources/additional_master_sources/ rules went with the agent pipeline —
# that folder is local-only now; see _archive/README.md.)
recipes/*.yml text eol=lf
data/sources/sources.csv text eol=lf
